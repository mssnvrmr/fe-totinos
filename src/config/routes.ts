interface RouterMap {
  HOME: string;
  LOG_IN: string;
  SIGN_UP: string;
  ORDERS: string;
  MENU: string;
  INGREDIENTS: string;
  USERS: string;
  PROFILE: string;
}

export const ROUTES: RouterMap = {
  HOME: '/',
  LOG_IN: '/log-in',
  SIGN_UP: '/sign-up',
  ORDERS: '/orders',
  MENU: '/menu',
  INGREDIENTS: '/ingredients',
  USERS: '/users',
  PROFILE: '/profile',
}