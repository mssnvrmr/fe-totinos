import type { ThemeOptions } from '@mui/material/styles';

export const customTheme: ThemeOptions = {
  palette: {
    mode: 'dark',
    primary: {
      main: '#00897B',
    },
    secondary: {
      main: '#FFB300',
    },
  },
};